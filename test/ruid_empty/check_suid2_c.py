from src import blick


def check_suid21_a():
    """No RUID"""
    yield blick.BlickResult(status=True, msg="No RUID")


def check_suid22_a():
    """No RUID"""
    yield blick.BlickResult(status=True, msg="No RUID")
